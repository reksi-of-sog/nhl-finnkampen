# Supabase Postgres connection string (service role recommended for server worker)
DATABASE_URL=postgresql://postgres:password@db.host:5432/postgres


# Optional: time zone you consider 'game day' for the nightly run (Helsinki)
TZ=Europe/Helsinki